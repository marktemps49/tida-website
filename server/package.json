{
  "name": "tida-credit-methodology-server",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for TIDA Credit Checker: receives uploaded deal documents, runs the methodology agent (CREDIT_METHODOLOGY.md) via the Claude API, and returns extracted scoring parameters.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.0",
    "mammoth": "^1.8.0",
    "multer": "^2.0.0"
  }
}
